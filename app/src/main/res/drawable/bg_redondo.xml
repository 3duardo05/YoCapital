<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The background color -->
    <solid android:color="#fff" />

    <!-- The Border Radius (corners) -->
    <corners
        android:radius="20dp"
        />

    <!-- The Border (stroke) -->
    <stroke
        android:color="#fff"
        />
</shape>